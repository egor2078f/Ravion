{
  "name": "ravion-stats-server",
  "version": "1.0.0",
  "description": "Statistics server for RAVION SCRIPTS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "backup": "node -e \"const { DatabaseManager } = require('./server.js'); new DatabaseManager().backupDatabase();\""
  },
  "keywords": ["stats", "analytics", "scripts"],
  "author": "RAVION",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
